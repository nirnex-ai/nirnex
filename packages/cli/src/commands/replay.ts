// Command: dev replay
// Parses args and prints "not implemented" until replay logic is wired.

export function replayCommand(args: string[]): void {
  console.log('[dev replay] not implemented', args.length ? `args: ${args.join(' ')}` : '');
}
