// Command: dev trace
// Parses args and prints "not implemented" until tracing logic is wired.

export function traceCommand(args: string[]): void {
  console.log('[dev trace] not implemented', args.length ? `args: ${args.join(' ')}` : '');
}
