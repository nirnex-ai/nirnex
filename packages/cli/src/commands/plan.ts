// Command: dev plan
// Parses args and prints "not implemented" until planning logic is wired.

export function planCommand(args: string[]): void {
  console.log('[dev plan] not implemented', args.length ? `args: ${args.join(' ')}` : '');
}
